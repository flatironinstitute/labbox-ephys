apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-{{ projectName }}-1
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 50Gi
  storageClassName: linode-block-storage-retain
